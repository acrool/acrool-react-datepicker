import { IDateTimepickerProps } from './types';
/**
 * DateTimepicker
 * 日期選擇器
 */
declare const DateTimepicker: ({ className, style, value, dateFormat, onChange, onClickOk, locale, minYear, maxYear, isDark, minDate, maxDate, isVisibleSecond, }: IDateTimepickerProps) => JSX.Element;
export default DateTimepicker;
